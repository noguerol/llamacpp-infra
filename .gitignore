# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local pi project config
.pi/

# Agent-only artifacts (plan files, project memory) — never commit
plan_*.md
pi.md
pi.md.local

# punched-memory — never commit project-local memory
.punched-memory.json
.punched-memory/
